public static String stutter(String word){
    String newWord = "";
    for (int i = 0; i < word.length(); i++){
        char ch= word.charAt(i);
        newWord= newWord+ch+ch;
    }
    return newWord;
}
