public static int pow(int base, int exponent){
    int product = base;
    
    for (int i = 0; i < exponent - 1; i++){
        product = base * product;
    }
    if (exponent == 0){
        product = 1;
    }
    
    return product;
}
